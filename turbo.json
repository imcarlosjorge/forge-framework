{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": [],
      "outputs": [],
      "cache": false,
      "persistent": true,
      "env": ["PORT", "JWT_SECRET", "VITE_PORT"]
    },
    "lint": {},
    "test": {}
  }
}
